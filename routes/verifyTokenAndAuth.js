// import verifyToken from "./verifyToken.js";

// const verifyTokenAndAuth = (req, res, next) => {
//   verifyToken(req, res, () => {
//     if (req.user.id === req.params.id || req.user) {
//       next();
//     } else {
//       res.status(403).json("Your are not authenticated");
//     }
//   });
// };

// export default verifyTokenAndAuth;
